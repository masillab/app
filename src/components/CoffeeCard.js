import React from 'react';
import { View, Text } from 'react-native';
import config from '../config.json';

const CoffeeCard = ({navigation}) => {
    return (
        <Text>hello</Text>
    )
}

export default CoffeeCard